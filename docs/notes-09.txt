notes for neural network hyperparameters:

-in general: F.relu necessary, because several linear function would result in one simple linear function, while several linear function with relu results in a one more complex function. relu after conv and linear layer
-use conv3x3 only to have one less parameter to worry about
-use pooling layers for translation invariance. (2x2 with stride 2)

-position of pooling layer. first pooling layer after only one conv layer instead of after two conv layer works better because then whole network is closer to vgg16 shape. getting narrow fast and then get deep
-only one linear layer works better than having two
-having more or less channel does not do a favor. ending with channel size 16 for conv is really good.however 8 is not much worse


TODO:

-1. read about data augmentation and what effects. then rotatate object random position etc.. open other DoF
data augmentation.
past main issueso the frist main issue: gripper hovered over objects because of non-potential rewards -> solution: potential reward
now the issue is that gripper moves towards object, but doesnt stop: augmentation necessary

introduced starting augmentation. still need augment starting rotation of last robot axis.
and still need to open other DOFs

-2. reward optimization

-3. try PPO

how to continue after the djungle trek:
1. understand which camera depth buffer to use
2. implement approach() function. first implement the function in the camera class, which detects the barriers. for that in the urdf file adjust the tcp geometry to see were the barrier is. z buffer, computer graphics, last thing I looked up



that was the TCP for testing for camera.approach_stop()

  <joint name="ee_tcp_joint" type="fixed">
    <parent link="ee_link"/>
    <child link="tcp_link"/>
    <origin rpy="0 0 0" xyz="0.14 0 0"/>
  </joint>

  <link name="tcp_link">
    <visual>
      <geometry>
        <box size="0.005 0.04 0.013"/>
      </geometry>
      <origin rpy="0 0 0" xyz="0 0 0"/>
    </visual>
  </link>







maybe approach_stop via position
